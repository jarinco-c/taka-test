# 手書き文字認識アプリ 仕様書

## 1. システム概要

手書きで書かれた文書（PDF・画像）をアップロードすると、テキストに変換して表示するWebアプリケーション。  
ブラウザから利用でき、変換されたテキストはコピーして他のシステムに貼り付けられる。

---

## 2. 利用技術

| 種別 | 技術 | バージョン | 費用 |
|---|---|---|---|
| Webフレームワーク | FastAPI (Python) | 0.111以上 | 無料 |
| OCRエンジン | EasyOCR | 1.7以上 | **完全無料・APIキー不要** |
| PDF処理 | PyMuPDF | 1.24以上 | 無料 |
| フロントエンド | HTML / CSS / JavaScript | - | 無料 |

> EasyOCRはローカルで動作するため、外部APIへの通信は一切発生しない。

---

## 3. 動作環境

### サーバー（アプリを動かすPC）

| 項目 | 要件 |
|---|---|
| OS | Windows / Mac / Linux |
| Python | 3.9以上 |
| ディスク空き容量 | 約1GB（EasyOCRモデルの初回ダウンロード分） |
| ネットワーク | 社内LAN接続（他のPCからアクセスさせる場合） |

### クライアント（使う人のPC）

| 項目 | 要件 |
|---|---|
| ブラウザ | Chrome / Edge / Firefox / Safari（最新版推奨） |
| ネットワーク | サーバーPCと同じLAN内 |

---

## 4. ファイル構成

```
プロジェクトフォルダ/
├── app.py              # バックエンド（サーバー処理）
├── requirements.txt    # 依存ライブラリ一覧
└── static/
    └── index.html      # フロントエンド（画面）
```

---

## 5. 機能一覧

### 5-1. ファイルアップロード

| 項目 | 内容 |
|---|---|
| 対応形式 | PDF / JPG / PNG |
| アップロード方法 | クリックして選択 または ドラッグ＆ドロップ |

### 5-2. ページ指定（PDFのみ）

| 指定方法 | 例 | 意味 |
|---|---|---|
| 省略 | （空欄） | 全ページ処理 |
| 個別指定 | `1,3,5` | 1・3・5ページのみ |
| 範囲指定 | `1-3` | 1〜3ページ |
| 組み合わせ | `1,3-5` | 1・3・4・5ページ |

### 5-3. 文字認識（OCR）

- EasyOCR により日本語・英語の手書き文字を認識
- ページごとにテキストを表示
- 認識結果はページ単位で「コピー」ボタンから取得可能

---

## 6. APIエンドポイント

### `GET /`
トップ画面（index.html）を返す。

### `POST /recognize`

| パラメータ | 種別 | 必須 | 説明 |
|---|---|---|---|
| `file` | multipart/form-data | 必須 | PDF / JPG / PNG ファイル |
| `pages` | string | 任意 | 読み取るページ番号（省略時は全ページ） |

**レスポンス例**

```json
{
  "total_pages": 3,
  "results": [
    { "page": 1, "text": "認識されたテキスト..." },
    { "page": 2, "text": "2ページ目のテキスト..." }
  ]
}
```

---

## 7. セットアップ手順

### 初回のみ

```bash
# 1. 依存ライブラリをインストール
pip install -r requirements.txt
```

> 初回インストール時にEasyOCRの言語モデル（約500MB）が自動ダウンロードされる。

### 起動

```bash
# 自分だけが使う場合
uvicorn app:app --port 8000

# 社内の他のPCからもアクセスさせる場合
uvicorn app:app --host 0.0.0.0 --port 8000
```

### アクセス方法

| 対象 | URL |
|---|---|
| サーバーPC本人 | `http://localhost:8000` |
| 社内の他のPC | `http://サーバーPCのIPアドレス:8000` |

---

## 8. 制限事項・注意点

| 項目 | 内容 |
|---|---|
| 同時処理 | 1リクエストずつ処理（同時に複数人が送信すると順番待ちになる） |
| 認識精度 | 極端に崩した文字・薄いインクは読み取れない場合がある |
| ファイルサイズ | 大きすぎるPDF（100ページ以上など）は処理に時間がかかる |
| 認識言語 | 日本語・英語に対応（中国語・韓国語等は非対応） |

---

## 9. 処理の流れ

```
【ブラウザ】
  ファイル選択・送信
      ↓
【サーバー (app.py)】
  PDFの場合 → PyMuPDF でページごとに画像に変換
  画像の場合 → そのまま使用
      ↓
  EasyOCR で文字認識
      ↓
  テキストをJSONで返す
      ↓
【ブラウザ】
  結果を画面に表示・コピー可能
```
